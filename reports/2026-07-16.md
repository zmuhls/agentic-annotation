# Daily maintenance — 2026-07-16

**Health:** Watcher up (com.zachmuhlbauer.hypothesis-watcher loaded, last exit 0), scripts compile, `poll.py` exits 0 (no new annotations). Cursor steady at 2026-07-06T02:37:04. `git pull` clean. The 04:42 `'list' object has no attribute 'get'` entry predates yesterday's fix (no crash-site suffix) — stale, not a live failure.

**Fixed — test suite was polluting the operational log.** `test_poll.py`'s two model-error cases call `process(..., dry_run=True)`, whose `except` branch hits `log()` — and `log()` is not `dry_run`-guarded, so every suite run appended `sonnet failed for test1 ... api down` lines into the live `logs/watcher.log` the daily health review reads (confirmed: a fresh pair landed today at 12:10). Added `tests/conftest.py` with an autouse fixture that redirects all seven of poll's mutable write paths (log, map, seen, cursor, actions, emergent, lock) to a per-test tmp dir. Re-ran the suite: 0 new log lines. Stripped the accumulated `test1` noise from the private log.

**Corrected yesterday's false claim + made tests runnable.** The 07-15 report said pytest was "already installed" — it is in no interpreter here (system Python is PEP-668 externally-managed). Added a minimal `pyproject.toml` (`[tool.pytest.ini_options]` only: testpaths=tests, `-q`) so `pytest` runs from repo root with no args, a README **Tests** section documenting the venv install path, and `.venv/`/`.pytest_cache/` to `.gitignore`. Full packaging + entry points stays a separate agenda item.

**Verify:** `py_compile` clean; **35/35 pass** (no args, via pyproject); `poll.py --dry-run` exit 0; log unpolluted; cursor/seen untouched. No private files staged.

**Dependencies:** none. `pytest` remains dev-only; no new runtime deps (stdlib only).

**Next candidates (agenda):**
- PRO-GRADE: MIT LICENSE, CHANGELOG.md, `ruff` config, GitHub Actions CI (pytest + ruff — suite is CI-ready now), then full `pyproject` packaging with console entry point.
- GENERALIZABILITY: move hardcoded `GROUP_NAMES`/`GROUP_NODES`/`ARTIFACT_DOMAINS`/`DH_DOMAINS`/`AIML_DOMAINS` out of `build_ia.py` into config, or fetch groups live from `/api/profile/groups`; move `user`/domains/paths to `config.json`.
- ADOPTERS: INSTALL.md (macOS launchd + Linux systemd/cron); script to bootstrap a starter `ia.md` from a new user's annotation profile.
- ROBUSTNESS: per-classification cost visibility (~$0.10 each) in the log; batch handling when many annotations arrive at once.
